//if in case we want to render the UI only once!
import React from 'react'

const HookCounter3 = () => {

    // We need to pass empty dependency array for renedering only once
  return (
    <div>

    </div>
  )
}

export default HookCounter3
